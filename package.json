{
  "name": "tema-madya-cms-sekolahku",
  "version": "0.3.0-rc.10.12.33",
  "private": true,
  "description": "Tema Madya CMS Sekolahku — tema pihak ketiga dengan gaya editorial institusional modern.",
  "scripts": {
    "dev": "npm run check:deps && vite --host 0.0.0.0",
    "build": "npm run check:deps && vite build",
    "check": "npm run check:source && npm run check:data && npm run check:style && npm run validate",
    "check:source": "node scripts/check-source.js",
    "check:data": "node scripts/check-demo-schema.js",
    "validate": "node scripts/validate-theme.js",
    "test": "npm run check:deps && playwright test",
    "test:ui": "playwright test --ui",
    "format": "biome format --write src/js src/css scripts tests playground",
    "format:check": "biome format src/js src/css scripts tests playground/app.js vite.config.js playwright.config.js",
    "dev:html": "npm run dev",
    "build:html": "npm run build && node scripts/build-html.js",
    "build:theme": "npm run validate && npm run build && node scripts/build-html.js",
    "check:deps": "node scripts/check-dependencies.js",
    "release": "npm run check && npm run build:theme && node scripts/release.js",
    "lint:js": "biome lint src/js scripts tests playground/app.js vite.config.js playwright.config.js",
    "lint:css": "biome lint src/css",
    "lint:php": "composer validate --strict && composer run lint",
    "check:style": "npm run check:style-config && npm run lint && npm run format:check",
    "format:php": "composer run format",
    "lint": "npm run lint:js && npm run lint:css && npm run lint:php",
    "check:style-config": "node scripts/check-code-style.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@tailwindcss/vite": "^4.1.0",
    "@biomejs/biome": "^2.2.0",
    "tailwindcss": "^4.1.0",
    "vite": "^7.1.0"
  },
  "type": "module",
  "dependencies": {
    "three": "0.185.1"
  }
}
